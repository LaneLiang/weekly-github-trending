
# selling points:
1. time-based sensing
reference-less, making the peripheral circuit simple.

2. Multi-level sensing
improve reliability and reduce bit error rate (BER)

power gating
enable the shaping inverter only at the sensing points to suppress the short-circuit current

3.charge injection suppression
charge injection-induced error-free

4. Successive Sensing and Checking
The proposed sensing scheme reconfigured by built-in-self-test (BIST) method to 


The proposed sensing scheme can be used for all two-terminal NVM technologies (STT-RAM, PCM, ReRAM), whose states are differentiated by the switching between a high resistance state (off-state) and a low resistance states (on-state).
